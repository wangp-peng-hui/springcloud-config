spring:
    profiles:
        active:
        - dev
        
---

spring:
    profiles: dev
    application:
        name: config-dev-wph
    

---

spring:
    profiles: test
    application:
        name: config-test-yyn